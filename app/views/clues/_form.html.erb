<%= form_for @clue do |f| %>
  <%= f.label :title, "Clue" %>
  <%= f.text_field :title %>
  <%= f.label :points, "Points" %>
  <%= f.text_field :points %>
  <%= f.label :category_id, "Category" %>
  <%= f.select(:category_id, @categories.map{|a| [a.name, a.id]}.unshift(["", ""])) %>
  <%= f.fields_for :question do |builder| %>
    <%= render "shared/question_form", f: builder %>
  <% end %>
  <%= f.submit %>
<% end %>
