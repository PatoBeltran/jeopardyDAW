<fieldset>
  <%= f.label :name, "Topic" %><br />
  <%= f.text_area :name %><br />
  <%= f.fields_for :categories do |builder| %>
    <%= render "category_form", f: builder %>
  <% end %>
</fieldset>

