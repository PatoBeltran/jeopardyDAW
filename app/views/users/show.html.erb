<% @games.each do |game| %>
  <div>
    <div>
      <%= game.topic.name %> |
      <%= link_to game.created_at.strftime("%B %d, %Y") , [current_user, game] %>
    </div>
    <div>
      <% game.members.each do |member| %>
        <%= member.memberable.name %>
        <div>
        <% if member.memberable_type == "Team" %>
          <% member.memberable.players.each do |player| %>
            <%= player.name %>
          <% end %>
        <% end %>
      </div>
      <%end %>
    </div>
  </div>
<% end %>
<div class="row">
  <div class="col-9 btn-row">
    <%= link_to "Add Game", new_user_game_path(@user), class: 'btn' %>
    <%= link_to "Clues", clues_path, class: 'btn' %>
    <%= link_to "Questions", questions_path, class: 'btn' %>
  </div>
</div>

<div class="">
  <div class="row">
    <div class="col-2">
      <h5>Topics</h5>
      <ul class="list">
        <div class="list-limited-size">
          <% @topics.each do |topic| %>
            <li><%= topic.name %></span> | <%= link_to "Edit", edit_topic_path(topic) %></li>
          <% end %>
        </div>
        <li><div class="btn-row">
          <%= link_to "Create new", new_topic_path, class: 'new-btn topic-color' %>
        </div></li>
      </ul>
    </div>
    <div class="col-2">
      <table>
        <thead>
          <tr>
            <th>Topic</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @topics.each do |topic| %>
          <tr>
            <td><%= topic.name %></td>
            <td><%= link_to "Edit", edit_topic_path(topic) %></td>
          </tr>
          <% end %>
        </tbody>
      </table>
      <div class="btn-row">
        <%= link_to "Create new", new_topic_path, class: 'new-btn topic-color' %>
      </div>
    </div>
    <div class="col-2">
      <h5 class="white">Categories</h5>
      <ul class="list">
        <div class="list-limited-size">
          <% @categories.each do |category| %>
            <li><%= category.name %></span> | <%= link_to "Edit", edit_category_path(category) %></li>
          <% end %>
        </div>
        <li><div class="btn-row">
          <%= link_to "Create new", new_category_path, class: 'new-btn category-color' %>
        </div></li>
      </ul>
    </div>
    <div class="col-2">
      <h5 class="white">Semesters</h5>
      <ul class="list">
        <div class="list-limited-size">
          <% @semesters.each do |semester| %>
            <li><%= semester.title %></span> | <%= link_to "Edit", edit_semester_path(semester) %></li>
          <% end %>
        </div>
        <li><div class="btn-row">
          <%= link_to "Create new", new_semester_path, class: 'new-btn semester-color' %>
        </div></li>
      </ul>
    </div>
    <div class="col-2">
      <h5 class="white">About us</h5>
      <ul class="list aboutUs">
        <li>Patricio Beltran</li>
        <li>Adrian Lozano</li>
        <li>Iliana Garcia</li>
      </ul>
    </div>
  </div>
</div>

<div class="row">
  <span class="col-4" style="text-align: left;">Jeopardy</span>
  <span class="col-4" style="text-align: center;">
    ©
    2014
    DAW
  </span>
  <span class="col-4" style="text-align: right;">Team 8</span>
</div>